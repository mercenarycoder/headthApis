const arr=[1,2,7,8,3,6,4,2];

arr.sort((a,b)=>{
    return b-a;
});

const ovj={
    a:1,
    b:2,
    c:3
};
obj[l]='jiaj';

const obj=Object.assign(ovj);

for([k,v] of Object.entries(obj)){
    v=v*10;
    obj[k]=v;
} 
console.log(obj);

// window.addEventListener('load',add.bind(2,3));
